node_modules/
.next/
.env
.env.local
.pglite/
coverage/
*.tsbuildinfo
.DS_Store
test-results/
playwright-report/
.lighthouseci/
next-env.d.ts

# Browser-automation scratch: Playwright MCP snapshots + screenshot/report output
.playwright-mcp/
output/

# Tier-3 behavioral eval results (opt-in, local only)
.skill-evals/

# Competitor / market research — local only, never committed
docs/research/

# Design-loop scratch: mockup variants and comparison boards are a
# conversation, not an artifact. The winner ships as components; these do not.
.design/

# Disposable review copies for the user (see tmp-review/README.md)
tmp-review/
